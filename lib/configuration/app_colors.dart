import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Colors.blue;
  // for light theme color schema
  static const lightByColor=Colors.white;
  static const lightDivColor=Color(0xFFF3F8FE);
  // for dark theme color schema
  static const darkByColor=Colors.grey;
  static const darkDivColor=Colors.grey;

}
